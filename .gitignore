# Python
venv/
__pycache__/
*.pyc
*.pyo
.env

# Pipeline output files
recent_verslagen.json
plenaire_verslagen.json
recent_vergaderingen.json
verslagen_with_content.json
verslagen_with_content_partial.json
verslagen_parsed.json
summary_*.json
!parliamentary-summaries/src/assets/summaries/summary_*.json
documents/

# batch_state.json is deliberately NOT ignored: the nightly workflow commits it
# so the next run can collect the batch it submitted.

# Angular
parliamentary-summaries/node_modules/
parliamentary-summaries/dist/
parliamentary-summaries/.angular/

# macOS
.DS_Store
